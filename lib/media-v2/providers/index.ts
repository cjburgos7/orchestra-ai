/**
 * media-v2 providers — Phase A stub.
 * Phase B: replicate-flux.ts, replicate-hailuo.ts (server-side only).
 */

export type { GeneratedMediaAsset } from "../types";

/** Phase A — no provider calls */
export const PROVIDERS_READY = false;
