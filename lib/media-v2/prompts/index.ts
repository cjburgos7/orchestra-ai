/**
 * Scene prompt compilation — Phase A stub.
 * Phase B: map VisualUniverse.sceneQueries → ScenePrompt.positive for Flux.
 */

export type { ScenePrompt, WorldMediaDNA, MediaGenerationRole } from "../types";

/** Placeholder — returns DNA shape for a category (Phase B will import visual-universe) */
export function compileScenePromptPlaceholder(): null {
  return null;
}
